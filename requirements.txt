# =====================================================
# DXploit – Advanced Offensive Security Scanner Toolkit
# =====================================================
# File ini berisi semua dependensi Python yang dibutuhkan
# agar DXploit dapat berjalan dengan lancar.
# Pastikan install dalam virtual environment (venv) agar
# tidak bentrok dengan package bawaan OS.
# =====================================================

# 🛠️ Cara Instalasi DXploit
# 1. Buat Virtual Environment      : $ python3 -m venv venv
# 2. Aktifkan Virtual Environment  : $ source venv/bin/activate
# 3. Install Dependencies          : $ pip install -r requirements.txt
# 4. Jalankan DXploit              : $ python3 main.py

             #------------------------#

# HTTP requests & API integrations
requests>=2.31.0
# Tabel rapi di CLI
tabulate>=0.9.0
# Integrasi Shodan API
shodan>=1.30.0
# Wrapper Nmap (untuk parsing hasil scan)
python-nmap>=0.7.1
# SSH remote connection / brute (post-exploit opsional)
paramiko>=3.4.0
# PostgreSQL client (untuk eksploitasi DB opsional)
psycopg2-binary>=2.9.9
# CLI output berwarna biar lebih mudah dibaca
colorama>=0.4.6
# Markdown reporting
markdown2>=2.5.0